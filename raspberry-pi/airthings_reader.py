import struct import time import bluepy.btle as btle class Wave2: """ Håndterer kommunikation med Airthings Wave sensoren via BLE. Klassen har ansvar for: - Scanning efter sensoren - Oprettelse af BLE forbindelse - Læsning af målinger - Lukning af forbindelsen """ CURR_VAL_UUID = btle.UUID( "b42e4dcc-ade7-11e4-89d3-123b93f75cba" ) def __init__(self, serial_number): self._periph = None self._char = None self.mac_addr = None self.serial_number = serial_number def is_connected(self): """ Kontrollerer om BLE forbindelsen stadig er aktiv. """ try: return self._periph.getState() == "conn" except Exception: return False def discover(self): """ Scanner efter Airthings sensoren. Sensoren identificeres ud fra serienummer. """ scan_interval = 0.1 timeout = 5 scanner = btle.Scanner() print("Scanning for Airthings Wave...") for _ in range(int(timeout / scan_interval)): advertisements = scanner.scan(scan_interval) for adv in advertisements: found_serial = parse_serial_number( adv.getValue( btle.ScanEntry.MANUFACTURER ) ) if self.serial_number == found_serial: print( f"Found Airthings device: {adv.addr}" ) return adv.addr return None def connect(self, retries=3): """ Opretter forbindelse til Airthings sensoren. """ tries = 0 while tries < retries and not self.is_connected(): tries += 1 if self.mac_addr is None: self.mac_addr = self.discover() if self.mac_addr is None: raise Exception( "Airthings device not found" ) try: print( f"Connecting to {self.mac_addr}..." ) self._periph = btle.Peripheral( self.mac_addr ) self._char = self._periph.getCharacteristics( uuid=self.CURR_VAL_UUID )[0] print("Connected") except Exception as e: print( f"Connect failed attempt " f"{tries}/{retries}: {e}" ) self.disconnect() self.mac_addr = None if tries == retries: raise time.sleep(2) def read(self): """ Læser aktuelle målinger fra sensoren. """ rawdata = self._char.read() return CurrentValues.from_bytes( rawdata ) def disconnect(self): """ Lukker BLE forbindelsen korrekt. """ if self._periph is not None: try: self._periph.disconnect() except Exception: pass self._periph = None self._char = None class CurrentValues: """ Indeholder aktuelle målinger. """ def __init__( self, humidity, radon_sta, radon_lta, temperature, ): self.humidity = humidity self.radon_sta = radon_sta self.radon_lta = radon_lta self.temperature = temperature @classmethod def from_bytes(cls, rawdata): data = struct.unpack( "<4B8H", rawdata ) if data[0] != 1: raise ValueError( f"Expected version 1 " f"but got {data[0]}" ) return cls( humidity=data[1] / 2.0, radon_sta=data[4], radon_lta=data[5], temperature=data[6] / 100.0, ) def to_dict(self): return { "humidity": self.humidity, "temperature": self.temperature, "radonShortTermAvg": self.radon_sta, "radonLongTermAvg": self.radon_lta, } def __str__(self): return ( f"Humidity: {self.humidity} %rH, " f"Temperature: {self.temperature} °C, " f"Radon STA: {self.radon_sta} Bq/m3, " f"Radon LTA: {self.radon_lta} Bq/m3" ) def parse_serial_number(manufacturer_data): try: device_id, serial_number, _ = struct.unpack( "<HLH", manufacturer_data ) except Exception: return None if device_id == 0x0334: return serial_number return None
