name: smart_air_app
description: "Smart Air Quality Monitoring System"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP requests til NestJS backend
  http: ^1.2.2

  # Socket.IO client til NestJS WebSocket Gateway
  socket_io_client: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
